// Divide function
function divide() {
    return 2000 / 100;
  }
  
  // Square arrow function
  const square = (x) => x * x;
  
  // Add arrow function
  const add = (x, y) => x + y;
  
  // Export the functions
  module.exports = {
    divide,
    square,
    add,
  };
